const Footer = (props) => {
  return (
    <div className={props.className}>
      <p>Made with <span>React</span> at <span>Le Reacteur</span> By <span>Clément</span></p>
    </div>
  );
};

export default Footer;
